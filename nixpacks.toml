[phases.setup]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build || true"]

[phases.install]
cmds = ["npm prune --production"]

[start]
cmd = "node server.js"

[providers.railway]
healthcheckPath = "/health"
healthcheckTimeout = 30
